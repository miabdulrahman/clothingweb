import FaqContent from '@/components/faq/FaqContent';

export const metadata = {
  title: 'FAQ — Sizing & Shipping Help | AUREN',
  description: 'Find answers to common questions about placing orders, shipping times across Sri Lanka, sizing guides, and return policies.',
};

export default function FaqPage() {
  return <FaqContent />;
}
